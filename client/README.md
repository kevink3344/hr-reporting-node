# HR Reporting Client

React/Vite frontend for the initial people and schools migration slice.

Run the API from the repository root, then start this client:

```powershell
npm install
npm run dev
```

The Vite development server proxies `/api` to `http://localhost:3000`.
